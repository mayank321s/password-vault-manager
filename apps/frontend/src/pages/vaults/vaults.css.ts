import { style } from '@vanilla-extract/css';

export const dashboardLayout = style({
  position: 'fixed',
  inset: 0,
  display: 'flex',
  flexDirection: 'row',
  overflow: 'hidden',
  backgroundColor: '#0f172a',
  fontFamily: 'inherit',
});
