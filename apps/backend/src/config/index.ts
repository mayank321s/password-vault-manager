import { appConfiguration } from './app.config';
import databaseConfig from './database.config';
import { jwtConfiguration } from './jwt.config';

export default [appConfiguration, databaseConfig, jwtConfiguration];
