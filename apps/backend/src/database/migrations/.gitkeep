// Example migration file - DO NOT RUN
// This is a template showing the structure of migrations
// Actual migrations will be created in Phase 2

'use strict';

module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Migration logic goes here
    // Example: Create a table
    await queryInterface.createTable('example_table', {
      id: {
        type: Sequelize.UUID,
        defaultValue: Sequelize.UUIDV4,
        primaryKey: true,
      },
      name: {
        type: Sequelize.STRING,
        allowNull: false,
      },
      created_at: {
        type: Sequelize.DATE,
        allowNull: false,
        defaultValue: Sequelize.NOW,
      },
      updated_at: {
        type: Sequelize.DATE,
        allowNull: false,
        defaultValue: Sequelize.NOW,
      },
    });

    // Example: Add index
    await queryInterface.addIndex('example_table', ['name']);
  },

  down: async (queryInterface, Sequelize) => {
    // Rollback logic - reverse the changes
    await queryInterface.dropTable('example_table');
  },
};
